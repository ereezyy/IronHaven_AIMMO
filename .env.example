# IronHaven AIMMO local environment
#
# Both keys are optional. If left unset, the app boots in offline mode
# (localStorage persistence, no multiplayer presence). Set both to enable
# Supabase-backed persistence + real-time MMO.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
